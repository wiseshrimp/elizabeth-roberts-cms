const embed = {
    name: 'embed',
    type: 'object',
    fields: [
        {
            type: 'string',
            name: 'code',
            title: 'Embed Code'
        }
    ]
}

export default embed